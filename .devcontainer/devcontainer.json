{
    "name": "WHA-HTTP",
    "image": "oven/bun:latest",
    "appPort": [4321, 4322],
    "containerEnv": {
        "rpm": "/usr/local/bin/rpm",
        "zevBot": "/usr/local/bin/zevBot"
    },
    "features": {
        "ghcr.io/devcontainers/features/node:1": {
            "version": "lts"
        }
    },
    "customizations": {
        "vscode": {
            "extensions": [
                "Oven.bun-vscode",
                "esbenp.prettier-vscode",
                "yy0931.vscode-sqlite3-editor",
                "astro-build.astro-vscode"
            ]
        }
    },
    "postCreateCommand": "bun install && apt-get update && apt-get install -y wget curl jq && npm install -g @vtsls/language-server && wget -O /usr/local/bin/rpm https://github.com/zevlion/rpm/releases/download/latest/rpm && chmod +x /usr/local/bin/rpm && wget -qO- https://github.com/zevlion/zevBot/releases/download/alpha/zevBot-linux-amd64.tar.gz | tar -xz -C /usr/local/bin/ && chmod +x /usr/local/bin/zevBot"
}
