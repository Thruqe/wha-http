name: wha-http

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      PORT: "8080"
      RPM_BIN: /usr/local/bin/rpm
      ZEVBOT_BIN: /usr/local/bin/zevBot
      JWT_SECRET: ${JWT_SECRET} # set in .env
    volumes:
      - data:/app/data # persist the SQLite db
    restart: unless-stopped

volumes:
  data:
